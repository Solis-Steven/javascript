// Pure functiones o funciones puras

// Una funcion pura es una funcion que retorna un dato
// pero no modifica los varoles de las variables

const duplicar = numero => numero * 2;

const numero1 = 20;

const resultado = duplicar( numero );